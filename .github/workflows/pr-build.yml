name: Docker & Python Build Check

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'greenbox_integration/Dockerfile'
      - 'greenbox_integration/uv.lock'
      - 'greenbox_integration/greenbox_bridge.py'
      - 'greenbox_integration/pyproject.toml'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (amd64)
        id: build
        uses: docker/build-push-action@v6
        with:
          context: greenbox_integration
          file: greenbox_integration/Dockerfile
          platforms: linux/amd64
          load: true
          push: false
          tags: temp/pr-build:latest

      - name: Test Python code Compilation
        run: |
          cd greenbox_integration
          pip install uv
          uv sync
          uv run python greenbox_bridge.py &
          PID=$!
          sleep 5
          if kill -0 $PID 2>/dev/null; then
            echo "Script started successfully"
            kill $PID
          else
            echo "Script failed to start"
            exit 1
          fi

      - name: Remove temp image
        run: docker rmi temp/pr-build:latest

